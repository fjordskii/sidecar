# Sidecar — instructions for agents

Follow @AGENTS.md. (AGENTS.md is the canonical instructions file — shared by every agent
that reads it, so they can't drift. This shim exists because Claude Code looks for
CLAUDE.md first.)
